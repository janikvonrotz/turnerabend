image: node:6.10.0

pages:
  stage: deploy
  script:
  - npm install
  - ./node_modules/.bin/gulp build
  - mv dist public
  artifacts:
    paths:
    - public
  cache:
    paths:
      - node_modules
  only:
  - master
